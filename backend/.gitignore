# Dependency directories
node_modules/

# Environment variables (contains sensitive keys)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*

# Uploads directory (except keep file if any)
uploads/
!uploads/.gitkeep
